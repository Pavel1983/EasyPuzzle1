using UnityEngine;

namespace PuzzleGame
{
    public class MainManager
    {
        [RuntimeInitializeOnLoadMethod]
        private static void Init()
        {
            //PuzzleGame.UI.UI.Instance.Open(GameConstants.StartScreen);
        }
    }
}
